# Resend
RESEND_API_KEY=re_L53pFnr2_BzsDhkrPtXKQx7tFN22jKpnf
RESEND_FROM="Acme <onboarding@resend.dev>"

# Admin Auth (JWT)
# - Required for `/auth/login` and Bearer access to `/auth/me`, `/admin/*`, `/providers/*`
GW_JWT_SECRET=tlPoGgSnNA3+6tAo7/C+2hGq6F0RTQJubCbyFRpZGzc=
# Optional: access token TTL (default: 8h)
GW_JWT_TTL_SECS=28800
# Optional: refresh token TTL (default: 30 days)
GW_REFRESH_TTL_SECS=2592000

# Bootstrap (first user registration)
# - When `users` table is empty, `/auth/register` requires this code.
GATEWAY_BOOTSTRAP_CODE=woshidamahou

# Frontend base URL for reset link
CAPTOK_BASE_URL=http://localhost:5173

# Backend base URL for reset link
GATEWAY_BASE_URL=http://localhost:8080

# Optional: reset path (default: /reset-password)
RESET_PASSWORD_PATH=/reset-password

# test: default admin username and pwd
USERNAME:mahougege
PASSWORD:mahougege
EMAIL:mahougege@gmail.com


